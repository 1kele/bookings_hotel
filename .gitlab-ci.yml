stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  before_script:
    - cp ${env} .env
    - cp ${env_test} .env-test
  script:
    - docker build -t booking-api-image .

deploy-job:
  stage: deploy
  script:
    - docker compose -f docker-compoes-ci.yml up -d
